<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/velocityPrimary</item>
        <item name="colorPrimaryDark">@color/velocityPrimaryDark</item>
        <item name="colorAccent">@color/velocityPrimaryDark</item>
        <item name="android:fontFamily">@font/open_sans_regular</item>
    </style>

    <style name="LoginTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/velocityBase</item>
    </style>

</resources>
